{
  "name": "jquery.smoothScroll",
  "version": "0.0.1",
  "description": "A jQuery plugin for smooth scroll",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Masunaga Ray",
  "license": "MIT",
  "devDependencies": {
    "grunt": ">=1.5.3",
    "grunt-contrib-jshint": "^3.2.0",
    "grunt-contrib-uglify": "^5.0.1"
  }
}
